#include <bits/stdc++.h>
using namespace std;
void ap(int arr[],int n){
    for(int i=0;i+1<n;i+=2){
        swap(arr[i],arr[i+1]);
    }
}
int main() {
    int arr[]={1,2,3,4,6};
    ap(arr,5);
    for(int i=0;i<5;i++){
        cout<<arr[i];
    }

    return 0;
}
